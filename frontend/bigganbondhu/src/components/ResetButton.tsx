import React from "react";

const ResetButton: React.FC = () => {
  return (
    <button className="px-4 py-2 rounded bg-red-500 text-white font-semibold hover:bg-red-600 transition">
      Reset
    </button>
  );
};

export default ResetButton;
